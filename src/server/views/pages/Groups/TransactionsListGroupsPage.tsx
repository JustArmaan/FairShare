import { Transaction } from '../transactions/components/Transaction';
import { type GroupMembersTransactionsSchema } from '../../../services/group.service';

export const GroupTransactionsListPage = (props: { group: GroupMembersTransactionsSchema[] }) => {
    return (
        
    )
};